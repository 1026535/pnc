# V08 — Alliance research categories, nodes and details

[Index and common contract](../PNC_VISION_MODULAR_PLAN.md). Depends on V01; reuse V04's node representation where the observed layout fits. Deliverable: one complete Alliance research inspection family, including donation-state facts.

## Evidence and owner

The September15 tour did **not** inspect Alliance research. Existing Alliance Home, member/manage and Hall recognition is not proof of this feature. Begin with current `pnc_observation_enricher.py`, `ocr_region_plan.py`, `domain/observation.py`, visual/control catalogs and `navigation_core.py`. Reuse the existing Alliance entry/return route.

## Implementation

1. Search saved artifacts and newer work for the research surface. If absent, take one bounded non-spending capture route on testing, provided its current membership/access permits it. Record category page, visible nodes, one detail and the actual close/back controls.
2. Define the feature's small typed result: category/node identity, level/progress, selection/lock state, visible contribution/donation options and costs/cooldown/counters only when observed. These facts are not authorization.
3. Measure layout/icon regions and request OCR within them. Reuse V04's node primitives when appropriate, but do not force a distinct Alliance card layout through Institute-specific coordinates.
4. Qualify independent screen/detail identity and current controls. Keep donation detail open for the feature; do not classify it as an auto-dismiss interrupt.
5. Complete Alliance Home → research → category → node/detail → research → Alliance/Home. Bind detail to the selected node and reacquire after scroll/category changes.

## Acceptance and proof

Add one coherent Alliance research captured integration test family alongside `test_alliance_captured_layouts.py`. Run real frames through both publishers and production OCR planning. Require exact node/detail association, readable progress/cost fields, safe clipping and no donation action produced from an ambiguous row.

Start `py tools/run_tests.py group unit.app.pnc.vision`, then affected checks. The same bounded capture route can supply live proof after implementation if its boundary was actually executed through the production navigator; otherwise run one corrected route. Save pre/post frames, membership/access outcome and typed trace. Stop before Donate, premium donation or research start. Do not join/change alliances or switch castles to gain access.

If evidence reveals several unrelated category layouts, complete one observed family and create explicit follow-on packets before expanding. Unavailable Alliance access is a named capture/route blocker, not successful support.
