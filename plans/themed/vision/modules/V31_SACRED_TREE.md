# V31 — Sacred Tree and blessing records

[Index and common contract](../PNC_VISION_MODULAR_PLAN.md) · [Building coverage](BUILDING_MENU_COVERAGE.md).
Depends on V01; V02 for Home entry. Status: planned; support is not yet certified.

## Outcome and current evidence

Own Sacred Tree menu, Blessing Record list and read-only record/detail views. Other-lord tree layouts are separate variants when required by current callers.

September14 reference/validation captures qualify the own-tree identity and Home return. PNC_SACRED_TREE_BLESSING_RECORD and PNC_OTHER_LORD_SACRED_TREE exist, but that alone does not qualify their content or live routes. Use the [versioned endpoint note](../../../../docs/game-reference/workflows/building-endpoints.md) as a route lead and current captured pixels as acceptance evidence.

## Canonical ownership and implementation

Sacred Tree definitions and record-row producer, bounded OCR/control catalogs, typed observation and existing navigation. Both `ObservationBuilder` and `NavigationPerception` use the same feature producer. Reuse V01's frame-bound identity/content/control contract; V02 handles Home acquisition, not menu semantics.

1. Preserve the current own-tree profile and publish displayed growth/progress, counters, timer and available controls without inferring harvest eligibility from a missing timer.
2. Measure Blessing Record rows; read observed actor/time/result fields and keep duplicate/clipped rows unresolved. Use exact identity fields when a consumer requires them.
3. Qualify the record/detail return. Distinguish own-tree action ownership from another lord's tree; add that variant only from an existing needed route and saved evidence.
4. Add or correct a scoped workflow note with source date/build, supported layouts, actual return and remaining gaps. Keep references separate from validation captures; preserve newer landed work before modifying it.

## Acceptance and bounded proof

Extend captured building-route tests using the two Sacred Tree fixtures, plus actual record frames through both publishers. Verify records stay associated with their rows and a remote-tree layout cannot inherit Harvest. Require independent visual identity, typed observed facts, measured unoccluded controls and correct post-close state. Unknown values never inherit a prior screen's facts.

Start `py tools/run_tests.py group unit.app.pnc.vision`, then use the index's affected/integration rule for actual source changes.

One configured-testing, active-castle core-runtime route: **Home → Sacred Tree → Blessing Record → own tree → Home.** Follow the common lease, fresh-frame and stop protocol; record screenshots, observations and action/return trace under ignored `.local-data/`. Saved evidence comes first; no broad live tour is required.

Do not Harvest, bless, water, claim or visit another account to create evidence. Unsupported remote-tree coverage is reported independently. If a material route remains unproved, report the exact pending edge rather than calling the feature complete.
