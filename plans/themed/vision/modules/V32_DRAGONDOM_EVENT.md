# V32 — Dragondom event-building menus

[Index and common contract](../PNC_VISION_MODULAR_PLAN.md) · [Building coverage](BUILDING_MENU_COVERAGE.md).
Depends on V01; V02–03 for occupied event-slot acquisition. Status: planned; support is not yet certified.

## Outcome and current evidence

The currently available Dragondom Conquest building endpoint, event hub and one read-only information/detail family.

The user confirmed a fixed event slot. The versioned endpoint note maps Dragondom to a Dragon Cave candidate but explicitly leaves its current screen identity unpromoted. Do not equate the old client name with today's UI title. Use the [versioned endpoint note](../../../../docs/game-reference/workflows/building-endpoints.md) as a route lead and current captured pixels as acceptance evidence.

## Canonical ownership and implementation

The existing DRAGONDOM_CONQUEST catalog entry, V03 occupancy proof, a scoped event producer with its own profile/control/region keys, and core navigation. Both `ObservationBuilder` and `NavigationPerception` use the same feature producer. Reuse V01's frame-bound identity/content/control contract; V02 handles Home acquisition, not menu semantics.

1. Require current present/visible slot evidence, then identify the actual endpoint from a saved or bounded capture. Add its exact screen/return contract before enabling the Home route.
2. Publish observed event title/phase, timer, progress/currency and unlocked information controls; a countdown alone does not establish availability or a start policy.
3. Implement one visible rules/progress/reward-preview family and its close/return. If a separate map/combat subtree appears, capture its identity and create a follow-on packet instead of building another navigator here.
4. Add or correct a scoped workflow note with source date/build, supported layouts, actual return and remaining gaps. Keep references separate from validation captures; preserve newer landed work before modifying it.

## Acceptance and bounded proof

Add real occupied-event hub/detail and unavailable/unknown-slot cases through both publishers. An old Dragon Cave source mapping or event-icon template is not current menu qualification. Require independent visual identity, typed observed facts, measured unoccluded controls and correct post-close state. Unknown values never inherit a prior screen's facts.

Start `py tools/run_tests.py group unit.app.pnc.vision`, then use the index's affected/integration rule for actual source changes.

One configured-testing, active-castle core-runtime route: **Testing Home → present Dragondom slot → current hub → one proved rules/info preview → hub → Home.** Follow the common lease, fresh-frame and stop protocol; record screenshots, observations and action/return trace under ignored `.local-data/`. Saved evidence comes first; no broad live tour is required.

Do not enter combat, enroll, spend event currency or claim rewards. If absent, finish the evidence inventory and leave that live/appearance qualification pending; do not wait for or activate the event. If a material route remains unproved, report the exact pending edge rather than calling the feature complete.
