# V39 — Pit and Rare Earth menus

[Index and common contract](../PNC_VISION_MODULAR_PLAN.md) · [Building coverage](BUILDING_MENU_COVERAGE.md).
Depends on V01; V02 for Home entry. Status: planned; support is not yet certified.

## Outcome and current evidence

Pit entry, Rare Earth overview/field or exchange information, and existing dispatch presentation as read-only evidence.

The catalog marks Pit non-upgradeable. The September14 Home target was observed outside the safe click band and was not tapped. PNC_PIT, PNC_RARE_EARTH_FIELD and PNC_DISPATCH are declared but do not prove the full current route. Use the [versioned endpoint note](../../../../docs/game-reference/workflows/building-endpoints.md) as a route lead and current captured pixels as acceptance evidence.

## Canonical ownership and implementation

Pit/Rare Earth-specific producer, catalog/navigation, shared spatial observations only for an actually evidenced field surface, and bounded profile/control/OCR catalogs. Both `ObservationBuilder` and `NavigationPerception` use the same feature producer. Reuse V01's frame-bound identity/content/control contract; V02 handles Home acquisition, not menu semantics.

1. Use V02 to acquire an unobstructed Pit body point and qualify the actual settled endpoint and Home return; do not reuse the rejected historical point.
2. Publish visible field/node identity, occupancy/status, currency/attempt counters and current inspection controls. Keep a distinct field/map layout out of Home's atlas model.
3. Qualify one non-spending field/rules/exchange preview detail. Parse saved dispatch forms only where already available; avoid adding a new map traversal engine or dispatch workflow.
4. Add or correct a scoped workflow note with source date/build, supported layouts, actual return and remaining gaps. Keep references separate from validation captures; preserve newer landed work before modifying it.

## Acceptance and bounded proof

Add current endpoint/detail captures through both publishers, retain the unsafe-HUD target negative and distinguish Pit from an upgradeable building. Require independent visual identity, typed observed facts, measured unoccluded controls and correct post-close state. Unknown values never inherit a prior screen's facts.

Start `py tools/run_tests.py group unit.app.pnc.vision`, then use the index's affected/integration rule for actual source changes.

One configured-testing, active-castle core-runtime route: **Home → Pit → one proved Rare Earth information/preview → Pit → Home.** Follow the common lease, fresh-frame and stop protocol; record screenshots, observations and action/return trace under ignored `.local-data/`. Saved evidence comes first; no broad live tour is required.

Do not Enter Field unless current evidence proves it is non-spending navigation, activate a pickaxe, dispatch, occupy/attack or exchange currency. An unknown entry outcome stops the route. If a material route remains unproved, report the exact pending edge rather than calling the feature complete.
