# V19 — Qualified World detections into current navigation

[Index and common contract](../PNC_VISION_MODULAR_PLAN.md). Depends on V01 and the current detector owner's qualification. Align with [Gathering feature03](../../gameplay/PNC_CORE_REMAINING_03_GATHERING_PLAN.md). Deliverable: one supported World object class published through the canonical spatial observation and consumed for non-spending inspection.

## Scope and current boundary

The 2026-09-22 center-region plan and qualification record below supersede this original V19 packet where they differ. Castle is accepted for bounded observation publication; tap, detail inspection and a completed live navigation route are still gated.

Tour01 identified standard World but published no objects in that observation path. This does not establish whether the cause was model availability, request scoping or publication. The task **Assess YOLO training progress** owns newer model evidence; inspect its current result before choosing work.

Existing owners: `core/vision/detection/yolo_onnx.py`, `app/pnc/vision/yolo_shadow.py`, `spatial_surfaces.py`, `world_map_coordinates.py`, `domain/observation.py`, World navigation helpers and `navigation_core.py`. Shadow detections are not automatically actionable. This packet does not retrain a model or replace map movement.

## Implementation

1. Establish the actual request/model/publication path on the saved World frame. Reconcile current model export, class mapping, runtime output contract and held-out PNC evidence with the model owner.
2. If a qualified PNC class is available, map its current bounds/confidence to the existing typed spatial object. Preserve explicit unknown class/level/relationship fields; crop OCR only for fields the selected inspection needs.
3. Enable that qualified output through the same production publishers where requested. Do not promote arbitrary generic pretrained labels or all shadow classes into actions.
4. Keep existing coordinate reading, calibrated movement and target query ownership. After a pan or coordinate move, reacquire on a fresh frame; do not reuse old boxes.
5. Bind a supported object's measured selection to its recognized non-spending detail and return. Dispatch, gathering, battle and march receipts remain feature03 work.

## Acceptance and proof

Target `test_world_spatial_objects.py`, `test_world_spatial_observation.py`, World coordinate tests and affected navigation/consumer contracts. Use held-out PNC class captures and a relevant negative; verify class mapping, frame geometry, unknown field handling and invalidation after movement through the production path.

Start `py tools/run_tests.py group unit.app.pnc.vision`, then affected checks. One core-runtime route: Home → World → acquire one qualified current object → its known read-only detail → World → Home. Include a single bounded pan only if needed to exercise changed reacquisition. Save model identity/class map, screenshots, typed detections and trace. Stop before attack, scout, dispatch or gathering.

If no qualified checkpoint/class contract is available, deliver the diagnosed publication gap and exact prerequisite to the model owner; keep actionable integration pending. V02 and all menu packets continue independently.

## Center-region YOLO integration plan — 2026-09-22

**Ownership and current gate.** This task takes V19 planning, implementation, review and delivery from **Coordinate Devin plans v1-v44**, which retains the central V-series ledger and other plans. **Assess YOLO training progress** owns the model, dataset, class qualification and failure triage. Its final reviewed training report at `artifacts/worktrees/yolo-training-dataset/.local-data/yolo_evaluation_v25/confirmed_training_20260922/lead_review/FINAL_REVIEW.md` found no class accepted at that time and no corrected-run candidate passed the unchanged selection gates. The later bounded V19 review described below accepts Castle **observation publication only** for the pinned original v25 export and central ROI. Tapping, detail identity and a full navigation route remain unqualified.

**Repository-proven baseline.** The recognized World branch of `pnc_observation_enricher.py` passes empty OCR lines to `build_world_map_spatial_surface`, explaining empty objects on that path. The existing `YoloOnnxDetector` is loaded only by the opt-in shadow tool. `ObservationBuilder` and `NavigationPerception` share `PncObservationEnricher`; coordinate-only movement intentionally publishes no objects. The read-only diagnosis at `d10761e8` changed no production code. Current `yolo_onnx.py` (SHA-256 `1e325f7bcf1d39e193c57eb972e472e31cde5c3e3542e29c07abb825640cd36b`) requires `end2end=True`, but the reference export declares `end2end=False` with embedded NMS. Its ONNX SHA-256 is `225f4c6f423d887bf116cc8b637ecd49d1cf10e8249a681d0a6b76f502463552`. This checkout's adapter rejects that export before inference. The historical raw float parity result and **failed** strict adapter parity (three unmatched PT and one unmatched ONNX detections) belong to the evaluated YOLO-worktree adapter, SHA-256 `349be1cc1fd835c811d054e1f3541b46b330f6c685a36233b41700c52a711405`, recorded in `artifacts/worktrees/yolo-training-dataset/.local-data/yolo_evaluation_v25/onnx_validation.json`. They do not establish parity for a later V19 adapter. Do not edit model metadata or treat training completion as class acceptance.

**First release.** Publish owner-qualified Castle detections as typed `DetectedSpatialObject` observations on a recognized, unobstructed World frame. Keep coordinate reading, calibrated movement, target query ownership and the coordinate-only fast path. A read-only detail inspection and return require separately qualified tap geometry, identity and a live route. Gathering, marching, scouting, battle and resource spending remain outside V19.

**Model upgrade seam.** `ObjectDetector` is the stable pixel-space inference interface. The training owner updates one typed `world_yolo_qualification.py` component with the accepted export hash, exact class order, thresholds, ROI version, review reference and class allowlist after its class-specific review. V19 constructs the producer from that contract and a local model path; World navigation retains the same request, ROI, HUD and frame gates across model revisions. The current record allows Castle observation publication only; a new training checkpoint or other class cannot silently become actionable. The model file itself remains an ignored local artifact, outside Git.

The runtime composition API is `load_world_yolo_producer(local_model_path)` → `build_application_runner(..., world_yolo_producer=producer)` → `CoreRuntime.observe(..., include_content=True, request=ObservationRequest.world_map_yolo_object_analysis())`. The constructor rejects an unreviewed model hash, class order or ROI version. The normal World request and coordinate-only movement path do not run YOLO.

Run the detector on the **original full screenshot**, preserving evaluated RGB letterboxing, class IDs and context. After mapping each box back to source pixels, require the **entire box** to lie inside a central rectangular region of interest (ROI) before it can be published for action. Center-only containment would admit clipped objects. Keep raw detections and rejection reasons in diagnostics. Cropping, resizing or masking the model input would change object scale/context and requires separate evaluation.

An initial ROI candidate is `x=[0.20W,0.80W]`, `y=[0.22H,0.72H]` (approximately `x=108..432, y=211..691` on the saved 540×960 World fixture). It contains the fixture's central castle and monster while excluding screenshot edges and much of the HUD. The detector owner's saved original-v25 validation-prediction diagnostic retained only **54/290 labeled objects (18.6%)** and excluded 236/290 (81.4%). It found 54 conditional true positives and no conditional false positives or misses among retained predictions, but full-frame recall after filtering is 54/290, and 42/54 retained ground-truth objects came from one correlated acquisition group. These conditional counts do **not** qualify a class or the runtime adapter. Evidence: `artifacts/worktrees/yolo-training-dataset/.local-data/yolo_evaluation_v25/confirmed_training_20260922/lead_review/roi_v19_provisional_diagnostic.json`. Before promotion, review held-out captures and supported UI layouts with the detector owner, report class and group coverage, then freeze normalized bounds and pixel rounding in one versioned policy. Re-evaluate any later boundary change. Popup, interior HUD and fresh-frame gates remain separate.

### Deliverables and acceptance

1. **Exact detector qualification.** With the model owner, select one class and record export/adapter hashes, unchanged 17-class ID order, operational confidence threshold, embedded/post-NMS behavior, pixel geometry and ROI version. Make the runtime adapter accept the *actual* export contract, rather than rewriting its metadata. Re-run raw and strict parity against the **final V19 adapter revision** and resolve or explicitly accept its measured differences; the historical adapter result cannot qualify a different implementation. Review positives and relevant negatives across acquisition groups, including edge, ROI boundary and interior HUD cases. Resolve relevant suspected benchmark-label errors in a separate versioned diagnostic while preserving historical canonical metrics. Report fixed-threshold class TP/FP/FN, localization and ROI-rejected ground truth. Castle now has bounded observation-only acceptance; farm, lumber camp, Hell Fortress and other classes remain unqualified. The owner's class-specific acceptance record is the promotion gate.
2. **One request-scoped production publisher.** Load the pinned local model once through existing app/runtime composition and fail clearly on an absent or incompatible model. Add explicit World-object inference scope to the existing `ObservationRequest` owner; do not equate every World OCR request with model inference. Invoke it only when that scope has recognized World identity, clear guard and coordinate viewport proof; retain the coordinate-only movement no-object/no-inference path. Apply the approved class allowlist, full-box ROI and visible HUD exclusions. Map the selected output to `DetectedSpatialObject` with typed source, bounds, confidence/model provenance, current `FrameRef`, and explicit unknown name, level, relationship and World coordinate where unproved. Both observation publishers use the shared producer; shadow reports remain diagnostic.
3. **Fresh inspection and return.** Select a qualified object from a fresh full World observation. If multiple objects share a class, preserve the selected box and refuse ambiguous retargeting. Require its measured tap point and current box to remain inside the ROI and unobstructed current frame. After any pan, coordinate move or guard change, discard old geometry and reacquire. Prove the selected object's expected read-only detail identity, then return through World to Home. If safe tap geometry or detail identity cannot be established, retain diagnostic publication only.
4. **Focused verification.** Test export metadata and source-coordinate conversion at supported 540×960 and 900×1600 layouts; full-box ROI boundary rejection; unsupported classes, popup/HUD and unknown-state suppression; both production publishers; coordinate-only no-inference; movement invalidation; multi-target ambiguity; and read-only inspection/return. Start `py tools/run_tests.py group unit.app.pnc.vision`, then `py tools/run_tests.py affected --base origin/main --explain`. Use the full runner only if a shared contract change warrants it. Frozen held-outs remain evaluation data, never training additions.
5. **One final-candidate live route.** Coordinate a target lease with the V-series lead, then use the configured testing instance's active castle through the canonical core runtime: Home → World → fresh eligible interior object → recognized read-only detail → World → Home. Include one bounded pan only if needed to prove reacquisition. Save pre/post full screenshots, typed observations, raw/eligible boxes and ROI, model/adapter identity, input/return trace and stop reason under ignored `.local-data/`. Stop on ambiguous identity, absent qualified object, stale frame, ROI/HUD conflict, unexpected detail or mutation boundary. A successful process exit or an older checkout is not live acceptance. Do not displace concurrent V08/V31/V44 live validation. Preserve **all** BlueStacks instances the user left open; do not close or restart them after lease release.

### YOLO failure handoff from live validation

For each distinct failure in the bounded route, save a **lossless full screenshot** and one manifest entry under this task's ignored `.local-data/`: image SHA-256/dimensions; timestamp/build and acquisition group; permitted target alias and World/UI/HUD state; export/adapter hashes and class order/thresholds; source-pixel ROI/version; raw and eligible detections with exclusion reasons; overlay/crop; observed detail or other ground-truth evidence; reviewer confidence; and near-duplicate/train/validation/test membership when known. Classify false negative, false positive, wrong class, localization, HUD and intended ROI exclusion separately. An ROI exclusion is not a detector miss; uncertain labels stay uncertain.

Send the manifest path and concise case summary to **Assess YOLO training progress** for reviewed, versioned dataset augmentation. Keep captures quarantined until that owner decides labels and split membership; never automatically retrain on a failure or move a frozen held-out sample into training. Send the final V19 revision, proof and any remaining blocker to the V-series lead for its central ledger.

### Castle qualification packet identified by the model owner

Castle was the first class evaluated. Existing original-v25 saved predictions at confidence 0.35 showed central-ROI Castle validation TP11/FP0/FN0 and test TP4/FP0/FN0, but repeated views and acquisition correlation limited independence; those exploratory counts alone did not qualify the final adapter or production path. The final-path replay used three positive test frames `frame_e8b0e7565d5ab3f1474e8493`, `frame_0febf2f66d24d73031450601`, and `frame_152be1ffcec5709bdd48785f`. It required zero Castle publication on test `frame_afd6bb362723a9398ce57611` (other central structures), validation `frame_b5940839de74e17083b776bd` (clipped edge castle), and test `frame_6420421de2bfa3d1c9e293b6` (opaque modal, requiring the guard). The owner accepted the final-path result for Castle observation publication only. A later live inspection must separately prove the selected Castle's safe tap point, expected detail and return; detection alone cannot establish relationship, level or detail route.

**Current V19 offline proof (2026-09-22).** Final integrated adapter SHA-256 `e5325032e7e2f03f4e08f6326a45e244481d04c66e284e4d468bcbad46b31f5c` loads original-v25 ONNX SHA-256 `225f4c6f423d887bf116cc8b637ecd49d1cf10e8249a681d0a6b76f502463552`. Frozen strict held-out parity still fails: 715/718 PT detections matched, three PT and one ONNX unmatched, all outside Castle; OOD 9/9 matched. The final request/publisher replay on the six owner-selected frames published one Castle on each positive and zero on each negative. A second replay used the corrected benchmark v2 manifest SHA-256 `71306782ca77cbe9e433fe6a9fac6cc6509435c68414a3ceaf4db5591b41e9cf` over all 61 validation/test images and five acquisition groups: 61 Castle GT, 15 whole-box ROI-eligible, 46 intentionally excluded, and final publisher TP15/FP0/FN0 at same-class IoU 0.5 among eligible objects. The 15 eligible GT are distributed across groups 1/6/2/4/2. Raw/rejected/published box evidence is saved under this worktree's ignored `.local-data/v19-evidence/`. The training owner independently reproduced the metrics against unrounded corrected-v2 GT and recorded **bounded Castle observation-only acceptance** at `artifacts/worktrees/yolo-training-dataset/.local-data/yolo_evaluation_v25/v19_consultation_20260922/castle_observation_evidence_review.json`; minimum matched Castle IoU is 0.917635. The checked-in class map now allows Castle publication. Tap/detail and live route remain separately gated. Conditional ROI success covers only 15/61 Castle annotations (24.6% full-frame coverage), across correlated groups; no truly empty-map held-out guarantee exists.

**Implementation review (2026-09-22).** The training owner reviewed implementation commit `219273e` and found one actionable spatial-tap bypass: a semantic Castle query could resolve a YOLO object and tap its box center without a qualified point. The executor now refuses YOLO objects through exact-object, semantic-query and explicit-point spatial actions; the owner rechecked this focused fix and found no further actionable bypass in the observation-only contract. Review evidence is at `artifacts/worktrees/yolo-training-dataset/.local-data/yolo_evaluation_v25/v19_consultation_20260922/completed_implementation_review_e774a75.md`. This closes the reviewed offline observation slice; it does not qualify tap/detail or replace the pending live World route.

**Castle interaction candidate after takeover (2026-09-22).** The V19 branch now contains an opt-in interaction contract pinned to the already qualified model and ROI, same-frame raw/eligible detection diagnostics, exact-object tap enforcement, and a read-only core workflow. Its source capture requires one interior Castle and a single OCR name label below that same box; it rejects a missing label, “My Territory,” or the active castle's name before input. A successful candidate opens Player Territory, follows the existing Player Info control, matches the displayed profile name to the source label, and observes the return through Territory to World before the core runner confirms Home. The default producer has no interaction contract, and the application entrypoint refuses before connecting when one is absent. The saved `farm_014.png` name crop was read by the current OCR backend; `farm_025.png` did not yield a name and remains non-actionable. These are offline crop observations, not live target qualification.

The V-series coordinator's read-only architecture rereview closed its source/detail identity and real-executor test findings; evidence is `.local-data/devin-vision-pipeline/v19-castle-review-20260922.md`. The model owner judged the saved box centers suitable only for **one diagnostic remote-Castle trial with explicit live authority and a lease**; promotion remains false and the own-territory sample is excluded. The popup owner returned the configured testing instance to Home and released its lease, but its roster content-confirm preflight exhausted its budget at roughly 36–38 seconds per observation. Active-castle identity is therefore unverified, and V19 has not made a live tap or acquired a lease. Do not repeat that unchanged failing preflight or enable the interaction policy until the identity prerequisite and model-owner review of actual before/detail/profile/return evidence are satisfied. Preserve all already-open BlueStacks instances.

**Offline delivery status (2026-09-22): complete and reviewed.** The pinned Castle observation publisher, center ROI, frame-bound diagnostics, and disabled-by-default Castle inspection candidate are implemented. The V-series architecture rereview closed its findings, a separate code review found no actionable issues, focused vision/route checks passed, and the portable full suite passed 2,909 tests with seven skips. This marks the offline slice complete; it does not qualify Castle interaction. The V-series coordinator owns the remaining active-castle identity preflight, one leased non-spending remote-Castle trial, model-owner review of detail/profile/return evidence, and any necessary fix and retest before enabling the interaction policy. Keep other YOLO classes outside this V19 acceptance.
