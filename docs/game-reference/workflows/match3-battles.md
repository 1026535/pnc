# Match-3 battle modes and exit behavior

Updated 2026-09-21 from offline inspection of packaged PNC **5.0.203 / versionCode 233**. Evidence root: `.local-data/apk-exploration/gameplay-lua/` in the main repository. No current live battle, Auto unlock or Daily-credit result was observed in this review. Downloaded updates may change the packaged behavior.

## Requested behavior

**User-confirmed, high confidence, 2026-09-21:** Campaign, Arena and Lost Land must each support our solver, early exit for fast Daily progress, and in-game Auto when unlocked. Time Rift was explicitly deferred. These are requirements, not evidence that every control or early-exit credit rule is qualified. The [match-3 epic](../../../plans/themed/gameplay/PNC_MATCH3_COMPONENT_PLAN.md) owns all delivery: M1 includes lifecycle and pure rules/combat selection, M2 implements Auto/Exit, M3 supplies feature/Daily bindings, and M4 integrates the solver and qualifies operation. The [solver design reference](../../../plans/themed/gameplay/PNC_MATCH3_BATTLE_SOLVER_PLAN.md) retains algorithm boundaries and rule evidence without separate milestones. V13/V14 and V30 require selectable-mode API handoff tests only at the battle boundary; their completion does not imply implemented or qualified battle modes.

## Client-source findings

All rows below are **artifact-observed, high confidence for this packaged build**. They describe client code, not current-server acceptance or measured UI geometry.

| Finding | Source under the evidence root | Automation implication |
|---|---|---|
| Auto is a toggle; the on-state has a different sprite and active text. | `uis/puzzlebattle/uis/puzzlebattlewin.lua`, `UpdateAutoBtn` (1188) and `OnAutoBtnClick` (1388). | Observe availability and on/off state. Tapping an already-on control would disable Auto. Solver input requires Auto off; Auto mode leaves move/skill selection to the game. |
| Campaign Auto has unlock checks driven by rules and player progress; the alternate path also considers VIP or an existing purchase. Failed checks show an unlock dialog. | Same file, `CheckAutoBattleIsOpen` (1577); `datas/puzzledata.lua`, `CheckOpenNewAutoBattle` (186). | Do not hardcode a universal chapter/VIP threshold or purchase an unlock. Locked/unreadable state yields an explicit unavailable/unknown result. An unlock dialog is owned information, not permission to buy. |
| Auto-next eligibility and saved continuation choices exist separately from current-battle Auto. | `datas/puzzledata.lua`, `AutoNextIsEnabled` (165); `puzzlebattlewin.lua` initialization (392–408). | Qualification must establish how to keep one authorized battle from starting another. Prove continuation disabled before entry or retain a blocker. |
| Exit opens a confirmation with battle-type-specific text and a specific callback. | `puzzlebattlewin.lua`, `OnExitBtnClick` (1315), especially Arena, God Maze and Adventure branches. | Treat Exit and confirmation as workflow-owned controls. Never use blind Back or a generic modal dismissal as the battle action. Similar button wording does not prove equal consequences. |
| The exit callback branches: several contexts set the play-movie-end flag; others dispose the controller and exit directly. It then requests cancellation. | Same file, `OnExit` (1358–1377). | Determine the actual context's skip/retreat/result behavior. The callback alone proves neither loss, refund, quest progress nor a safe return. Do not infer that the entire battle is a noninteractive replay from this branch. |
| Ordinary battle initialization sets 5 × 7; separate PVP initialization sets 7 × 7. Report-parser controller selection distinguishes PVP, Collect and ordinary paths. | `datas/puzzledata.lua`, ordinary `Battle` (365 onward), PVP initialization (452 onward); `uis/puzzlebattle/battles/battlereport/puzzlebattlereportparser.lua`, `InitReport`. | Qualify the actual Arena subtype and control/rule semantics. Do not map a human-facing context name to a board size or borrow Campaign combat assumptions without evidence. |

The original source-derived board findings remain: adjacent ordinary swaps must form the supported matches; specials are clicked. Straight five or a qualifying five-cell T/L creates color-clear, straight four creates a 3 × 3 clear, and a 2 × 2 square creates a five-cell cross clear, with color > nine-grid > cross creation precedence. Sources: `puzzleboard.lua` `CheckHasBoom` / `CheckColorBoom` / `CheckNineGridBoom` / `CheckCrossBoom` / `CheckHasSpecialBoom`, and `puzzlecell.lua` drag/click handlers, under `uis/puzzlebattle/battles/battleunits/puzzles/`. These are **artifact-observed** for the inspected rule path; exact server spawn placement, chaining, refills and per-context application remain unqualified. The existing plan also records the user's confirmation of the three special effects. The retained cross-effect contract clears the activated cell and its four immediate orthogonal neighbours, clipped at board edges; it does not clear the full row and column.

## Repository facts and open boundaries

- **Repository-proven, high:** `CampaignExecutionMode` in `app/pnc/domain/daily_maintenance.py` selects `fixed_stage` or `progress_then_farm`. The new battle-mode choice is separate from this target-selection policy.
- **Repository-proven, high:** `pnc_automation/app/pnc/domain/match3_solver/` (2026-09-22) is the pure board-rule/one-step-decision baseline: immutable models, a canonical detector for runs, the explicit square rule and creation precedence, the three activation footprints, and a deterministic uncalibrated ranking. It carries no observations, lifecycle, damage model or runtime wiring. All context/mode availability remains unavailable; each mode's availability is governed by its own lifecycle and policy qualification (observation, battle entry/return, Exit/Auto behavior), independent of solver-ranking calibration.
- **Repository-proven, high:** `daily_quest_catalog.py` groups Campaign challenge/AP/chapter-clear labels under one quest ID. `HERO_ARENA` labels and the Daily plan refer to Hero Showdown; that is not proof they map to the requested match-3 Arena surface. Preserve the exact observed objective and qualify the route before associating a battle with it.
- **Repository-proven, high:** the [Daily plan](../../../plans/themed/account-runtime/PNC_DAILY_CASTLE_MAINTENANCE_IMPLEMENTATION_PLAN.md) records Lost Land static route captures and requires Daily-row advancement after Challenge. It does not establish early-exit credit. Lost Land, Land of Trial and V33 Lost City Headquarters are separate scopes.
- **Unknown:** exact current Lost Land source-family mapping, its qualified active-battle/exit/Auto controls and outcome rules; current Arena subtype; context-specific retreat cost/refund/credit; current Auto unlock state. Names alone do not settle these questions.

The next useful evidence is a saved or separately authorized context-specific before/after sequence: exact entry and target, active battle and controls, any confirmation, actual result/return, and the exact Daily row before/after when testing fast exit. An Auto sequence also records unlocked/on-state and absence of automatic next-battle entry. Reuse adequate shared-control evidence; do not spend to populate every context/mode combination merely for bookkeeping. The retained Campaign one-attempt/20-AP allocation is not a budget for the additional contexts or modes.

## Current Campaign pre-entry observation, 2026-09-22

**Live-observed visual fact, current installed build (build number not captured):** on the configured `testing` instance, a tap on the glowing current node of the Chapter 6 path opened a panel titled `[6-5] Marsh of Tear`. The panel visibly showed 126/120 AP, Challenge cost 12 AP, a Challenge control, a Close X and an enemy lineup. No Auto-next control was visible on this panel. Source frame: `C:/Users/lebel/pnc/artifacts/2026-09-22/testing/20260922T235029Z_core_20260922T234747Z_82d60eea_0045_c2_stage_after_0.png`. This proves only the displayed panel; the persisted Auto-next preference remains unknown and no formation or battle was entered.

**Repository/runtime boundary:** integrated candidate `d1afe897f54396c93acb2789a2a32c8dbb0fa3c4` recognized the Chapter 6 path and its owned Back, but classified the stage-detail frame above as `PNC_POPUP`. Generic interruption recovery tapped the panel's Close X and returned to the chapter path. The stage tap itself succeeded; qualified stage-detail provenance and formation entry remain pending the V14 recognition/ownership correction. The coordinator's ignored QA record is `.local-data/live-test-batches/m1-v14-integrated-preentry.md` in the `match3-v14-live-candidate` worktree. Zero resources were spent.

**Artifact-observed for packaged build 5.0.203/233:** ordinary match cells create colored attack armies at their board columns (`puzzlebattlecontroler.lua:147-170`), and the enemy map picks the frontmost living row covering each column (`puzzleenemymap.lua:90-119`). Same-row overlap can yield multiple front enemies. The client report supplies actual target IDs, damage and resulting HP (`puzzleserverreportcontroler.lua:337-421`), so a column exposure is only a targeting opportunity, not a damage or kill forecast. The army recovery path sends MP effects to living heroes of the same color (`puzzlebattlecontroler.lua:226-258`). The pure `match3_solver.combat` selector uses those column, countdown and color opportunities with an explicit `column_front` routing contract. It is an uncalibrated one-step heuristic; observed health, exact damage, color modifiers, enemy retaliation and refill outcomes still need current traces before a win/survival model can be qualified.
