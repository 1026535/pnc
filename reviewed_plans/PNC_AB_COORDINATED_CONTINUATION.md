# A/B coordinated completion plan

## Objective and authority

Resume and finish the original combined workflow-porting and non-YOLO recognition
workload with distinct ownership. This is sustained implementation, not merely cleanup,
publication of completed work, or another audit. The user requested this coordinated
revision and handoff on September 12, 2026, with one Luna coding agent per supervisor.

This plan supersedes older pause instructions and duplicated ownership in the two task
plans. Original behavior requirements remain unless a row below explicitly reassigns or
excludes them. A owns [workflow execution](PNC_CORE_WORKFLOW_PORTING_PLAN.md); B owns
[recognition implementation](PNC_NON_YOLO_RECOGNITION_PLAN.md). Those files reference this
table instead of copying shared contracts.

Both supervisors may implement their assigned scope, run proportionate offline checks,
create isolated feature branches/worktrees, commit and ordinarily push their reviewed
slices. A alone owns history-preserving combined-candidate integration and publication.
This handoff does not itself grant mainline landing authority; A may use a separately
received explicit user authorization, otherwise publish the candidate and continue other
independent slices. B must not land to main under older recognition ownership. No force
push, destructive cleanup, live game input, account/castle change, message send, spending
or scheduler activation is granted here. Old one-shot live budgets are not renewed.

Milestones are checkpoints, not mandatory approval stops. Continue until assigned
implementation is complete or a concrete dependency/authorization/evidence blocker
prevents the next slice. Complete independent work while another slice is blocked.

## Verified starting point

- Fetched main: `2eacb126cfbcb943c08d78104df2200ab9bae9e0`.
- Published combined candidate: `codex/workflow-recognition-integration` at
  `8a7d1a9d259120544c5382d246f810602527839d`, worktree
  `.local-data/worktrees/workflow-recognition-integration`.
- Castle `cd3503c0`, Campaign `1b9e5ceb`, and Research producer `6c48073b` are included
  in main. Old component remote tips need not be moved to prove publication.
- Candidate adds Daily plan `cdf308a3`; its production tree is the same as that main.
- A reported combined review clear, 195 focused passes, 1,885 portable passes and six
  explained skips (five optional screenshots, one Windows symlink privilege), and a
  successful ordinary candidate push. Reuse these results only for unchanged behavior.
- Catalog union is 37 profiles. Recompute if the base changes; retain legitimate additions.

Fetch and inspect only changes since this checkpoint when starting. Do not repeat the
broad audit, rebuild completed foundations, replay old spending, or clean historical
worktrees. Planning artifacts describe historical runtime state, not current game state.

## Combined backlog and one owner per deliverable

Statuses separate implemented code from live proof. `Blocked proof` does not mean that
completed code should be discarded, or that unsupported behavior may be promoted.

| ID / original deliverable | Owner | Disposition and next acceptance | Dependency / live limit |
|---|---|---|---|
| A01 Ensure game running | A | Completed typed lifecycle; retain foreground/readiness and bounded loading tests. | Broader cold-start/login proof is not inferred. |
| A02 Popup recovery | A | Completed canonical execution/recovery lifecycle; retain no-replay behavior. | B owns detection facts; no local popup bypass. |
| A03 Login | A | Remaining legacy port: preserve canonical params/credentials and preparation behavior; typed direct/authored bindings and failure tests. | B supplies missing login/overlay facts; no live credential entry or account change here. |
| A04 Select castle | A | Completed implementation and real postflight regression; no-op live proof retained. Close only demonstrated offline gaps. | Alternate target switch and return proof blocked on explicit target authorization. |
| A05 Refresh roster | A | Completed typed scanner, persistence and caller bindings; retain exact identity and full-scan tests. | No second scanner or automatic authored-alias repair; scheduled freshness project remains separate. |
| A06 Send Alliance chat | A | Completed shared send operation and authored binding; retain receipt tests/proof. | No message resend to reproduce old proof. |
| A07 Send World/Kingdom chat | A | Completed shared send operation and authored binding; retain channel/draft/receipt tests. | No message resend. |
| A08 Send mail | A | Remaining legacy port: typed compose/recipient/body flow, single send and meaningful receipt, direct/authored parity. | B owns compose/receipt observation producers; exact live destination/content authorization absent. |
| A09 Collect mail | A | Completed typed workflow/callers; retain persistence and unavailable-mailbox semantics. | Unavailable-mailbox proof is not proof of reading a new message. |
| A10 Collect Kingdom chat | A | Completed typed workflow/callers and archive integration; retain tests. | Archive protocol remains with existing store owners. |
| A11 Open building | A | Completed shared core and modeled endpoints; retain bounded focus/camera scan. Inventory only required remaining endpoints from original callers, then close evidence-supported gaps. | B owns each endpoint's profile/control evidence; unmodeled/unbuilt screens remain explicit, not generic-success fallback. |
| A12 Building construction | A | Remaining port of existing construction behavior, typed operations/callers and strict observed completion. | X01 plus B-owned menu/queue/control facts; resource proof separately authorized. |
| A13 Building upgrade | A | Remaining port of existing upgrade policy, prerequisite handling and completion, retaining useful tests. | X01 plus B-owned normal/premium/queue facts; consult game-reference note. No extra spending or new upgrade policy. |
| A14 Research | A | Port the existing workflow after X01; consume B's completed Start/current-active facts and retain strict postconditions. | No duplicate tree parser or rewrite to hide a producer gap. Additional research spending not authorized. |
| A15 Gathering | A | Port existing navigation/formation/dispatch behavior through reviewed operations and reliable postconditions. | X01, B04. Do not silently implement the separate Daily cavalry/search backlog as part of a port. |
| A16 Campaign | A | Assets, entry mapping and stage/chapter/map return edges completed; retain tests. Port existing stage-to-battle-preparation behavior. | B05 and X01 only for actual mutating operations. Automated entry/return proof pending; full battle automation belongs to Daily unless already explicitly in original scope. |
| A17 Existing Daily maintenance execution | A | Port existing coordinator/session/claims and implemented resource-item/Hero Hall adapters to the canonical core boundary; preserve parser, journal, cooldown checkpoints and outcomes. Status-only workflow already complete. | X01 and relevant B facts. New Daily capabilities, release policy, target repair and scheduling remain in Daily plan. |
| X01 Canonical mutation bridge | A | Critical path: bridge existing exact authority, observed executor and journal into typed workflow lifecycle; prove pre-dispatch denial, durable intents, reconciliation and no replay with offline tests. | B provides facts only. No alternate approvals/journal or effect mislabelling. |
| B01 Original phase A: coverage inventory | B | Refresh enabled/unsupported/unused dispositions once from current callers. Preserve canonical IDs; identify every remaining active family, reference and evidence gap. | Historical 329/336-selector and 90-family counts are checkpoints, not current acceptance targets. |
| B02 Original B0/B: provenance, decisions, guards | B | Foundation and Research real-builder correction completed. Retain same-frame publication in both paths; fix only concrete remaining shared fact/ownership gaps. | A owns action dispatch/recovery execution. Coordinate domain changes via the interface procedure below. |
| B03 Original C/D: catalog, fixed controls, OCR | B | Retain matcher, manifest, immutable OCR context, strict numeric parser, compiled region plans and bounded fallback. Complete required missing producer/region migrations by family. | Guarded full-frame fallback is valid until evidence supports cropping; do not convert every fallback merely to reach a count. |
| B04 Original F: Gathering/March | B | First producer priority: existing node/formation/confirm/slot facts, controls and guarded post-dispatch observations through both real paths, using saved evidence. | A15 consumes. Never dispatch a march or invent unavailable formation evidence. |
| B05 Original F: Campaign | B | Independent Campaign workflow/graph implementation reassigned to A. Maintain A's assets and add missing screen/control/content facts demanded by the existing Campaign workflow. | A16 owns route/endpoint/behavior. Retain all useful Campaign profiles. |
| B06 Original F: Research/categories | B | Development tree, measured node selection, guarded normal Start and strict active-detail publication completed. Retain real-builder regression. Remaining category facts require evidence; A owns category navigation. | Historical Construction I live action is retained, not repeated. No premium-control promotion. |
| B07 Original E1/E2: Quest and Bag | B | Row associations, parser isolation, provenance and later live proofs retained. Close only demonstrated current gaps (clipping, action state, requested content). | A17 owns traversal/claim behavior. No second inventory/Daily parser. |
| B08 Remaining active families | B | Enumerate from B01 and finish required login/mail/building/member-only/field producers and original supported target/build/locale evidence cells. Prioritize A03/A08/A12/A13 dependencies. | No arbitrary new locales/aspects or new game tasks. Missing source evidence gets a precise blocker, not fabricated controls or a success label. |
| B09 Independent qualification/package/performance | B | Retain original independent labels, held-out groups, action/row containment, installed-asset and latency gates. Run missing gates against stable slices with recorded environment. | Do not repeatedly benchmark unchanged code or claim broad accuracy from reference transforms; unavailable live cells stay blocked. |
| X02 Combined integration | A | Review and publish coherent producer/consumer combinations, reuse main's atomic journal/locking and Chat/archive contracts, final portable validation. | B publishes component commits; A alone integrates. Cleanup is not part of completion. |
| D01 Broader Daily completion | Future Daily owner | Separate plan retains new feature implementations, cavalry-specific gathering, Campaign battles, canary promotion, role/cache repair, recurring authority and scheduler. | A17/X01 supply the migrated foundation; do not absorb D01 into A/B silently. |

Original B phase labels A/B/C are phase names, not ownership of these two conversation
tasks. Every phase's perception work belongs to task B. YOLO/trained detectors, OCR-engine
replacement, unrelated archive/host projects and speculative new workflows remain excluded.

## Shared interface and file ownership

B publishes canonical typed facts and measured controls: final screen/layout and guard
state, capture/input provenance, supported control eligibility, row identity/content,
and relevant post-action state. Missing, conflicting or blocked evidence stays explicit;
absence alone does not mean zero, free, completed or authorized. Screen identity alone
does not authorize a mutation. Publish applicable facts consistently through
`ObservationBuilder` and `NavigationPerception`; preserve coordinate-only proof semantics.

A consumes those observations via `WorkflowContext`/core operations and the canonical
mutation boundary. A owns workflow postcondition decisions; B owns the reusable predicates
supporting them. Retain `DailyMutationAuthorizer`, `JournaledMutationDispatcher`,
`DailyRunJournalStore`, the observed executor and canonical lifecycle/leases.

Fixed edit ownership: A owns engine/workflow/entrypoint code, runtime composition,
navigation graph and operations, action requests/effects, workflow policies, caller
migration and combined integration tests. B owns domain observation models and facts,
screen/control identifiers and selector declarations/registry, vision/classifier/catalog
code and assets, observation producers/requests/provenance, OCR, producer tests and
benchmark tooling. Existing storage/lease internals are reused, not redesigned. In mixed
files these symbol responsibilities still apply; an inseparable change is assembled by
A during integration from B's reviewed producer changes. Do not edit a peer's worktree.

B decides and publishes observation interfaces through the existing canonical models;
A consumes published facts. Preserve existing consumer semantics. Record each coherent
producer batch's public symbols, unknown/blocked behavior, fixtures and acceptance evidence
once in B's plan/report, then send A one ready-to-integrate notice with its commit.
Consumer approval before each producer slice is not required. A integrates at its next
useful dependency boundary, without waiting for B's whole backlog.

A records missing facts once in its dependency list and continues unblocked work. Send
one consolidated request when a missing contract materially blocks progress or changes
the critical path; do not repeat already recorded requests. Actual breaking changes,
unresolvable integration conflicts and safety issues warrant a concrete escalation.
Routine startup/status/acknowledgement exchanges, peer polling and per-slice ownership
negotiation are unnecessary. User-facing progress updates remain separate. These process
rules do not authorize live actions or expand implementation scope.

This September 12 correction applies forward: retain contracts already settled and work
already underway. Do not restart the inventory or renegotiate Login/Gathering evidence
gaps merely to adopt it. Missing visual evidence remains a real dependency, not a reason
to invent a control, weaken a receipt or negotiate repeatedly.

## Execution and usage discipline

Each existing task remains the supervisor and uses at most one active
`gpt-5.6-luna` coding sub-agent at `xhigh` reasoning. Reuse that worker for successive
bounded slices; do not create a new user-visible task for each slice or a review swarm.
For grunt work, offload long-running, repetitive, specific or concrete work that consumes
many tokens but does not require complex reasoning to that Luna xhigh agent. The supervisor
remains in charge of complex or uncertain work, analysis, reasoning and orchestration.
The responsible supervisor defines its contract and acceptance checks before delegating;
this does not require a peer approval round. Review the worker's diff
and evidence rather than treating its completion message as acceptance. Luna implements
concrete code/tests in the supervisor's assigned isolated worktree. Do not delegate an
unresolved architecture decision merely because its implementation will be lengthy.
Luna does not perform live actions, publish main or modify another owner's worktree.

A owns X01 and evidence-supported consumer migrations; if Login lacks controls, continue
the existing Daily adapter or another unblocked port. B owns the once-only B01 inventory
and coherent producer-family batches, starting with evidence-supported Gathering/March
controls and the recorded login/mail dependencies. Reorder remaining independent rows by
actual dependency and evidence availability, without restarting completed or active work.

Work in workflow-named `codex/` feature branches for A and a recognition continuation
branch for B. Start from the published coordinated plan commit (or a descendant containing
it); preserve the historical worktrees. Do not both edit the shared main checkout.
Publish complete reviewed slices and integrate at useful producer/consumer boundaries,
not on every tiny edit and not only after the entire backlog. Report compact row status,
changed contracts, commit IDs and concrete blockers. Do not poll or repeat full audits.

Use focused/affected tests during development and the final portable full suite for
shared-contract/fallback/combined acceptance per repository guidance. Reuse valid evidence
when code/policy/selectors have not changed; invalidated dependencies require targeted
reassessment, not automatic repeated live spending. Original B performance qualification
is in scope, but measure only stable changed families without concurrent heavy runs.

Live acceptance is tracked separately. These handoffs authorize offline implementation;
for missing live proof, prepare an exact proposed account/role, action/target/budget and
stop condition and request the necessary authorization while independent coding continues.
One designated operator holds the canonical lease across any subsequently authorized live
phase. Luna workers remain offline. Never claim the whole workload live-complete while
required live cells are blocked.

## Completion

Every row must end with implemented/integrated evidence, an explicit scope disposition,
or a concrete external blocker. Distinguish blocked dependencies from completed behavior.
Do not mark unsupported active families as complete merely because they abstain safely.
Stop implementing a row when its required behavior and proportionate checks pass; continue
the next unblocked row. A publishes the final combined result and hands the remaining D01
work to a future Daily task. No cleanup, scheduler activation or invented feature work is
needed to finish this coordinated workload.
