# V43 — Goddess Statue menu and information

[Index and common contract](../PNC_VISION_MODULAR_PLAN.md) · [Building coverage](BUILDING_MENU_COVERAGE.md).
Depends on V01; V02 for Home entry; V16 for shared upgrades. Status: planned; support is not yet certified.

## Outcome and current evidence

Goddess Statue overview, displayed attributes/progress and one read-only skill/stat/requirement detail supported by current evidence.

PNC_GODDESS_STATUE and exact level publication exist; the previous recognition work qualified bounded level fields. The tour did not qualify deeper Statue menus or any activation behavior. Use the [versioned endpoint note](../../docs/game-reference/workflows/building-endpoints.md) as a route lead and current captured pixels as acceptance evidence.

## Canonical ownership and implementation

Goddess-specific content in the existing building producer, profile/control/region keys, canonical level/requirement models and core navigation. Both `ObservationBuilder` and `NavigationPerception` use the same feature producer. Reuse V01's frame-bound identity/content/control contract; V02 handles Home acquisition, not menu semantics.

1. Preserve the current level/control contract, then qualify actual subpanel identity and displayed attributes/progress without assuming a specific blessing/skill system.
2. Publish observed effect/level, requirements/costs and selected-state facts for the evidenced information family. Keep current state distinct from next-level preview.
3. Measure its owned close/return and separate activation, upgrade or claim controls from inspection. Create no generalized buff policy.
4. Add or correct a scoped workflow note with source date/build, supported layouts, actual return and remaining gaps. Keep references separate from validation captures; preserve newer landed work before modifying it.

## Acceptance and bounded proof

Extend existing building-level/captured tests with actual Statue information/detail frames through both publishers and a wrong-building/preview-state negative where evidenced. Require independent visual identity, typed observed facts, measured unoccluded controls and correct post-close state. Unknown values never inherit a prior screen's facts.

Start `py tools/run_tests.py group unit.app.pnc.vision`, then use the index's affected/integration rule for actual source changes.

One configured-testing, active-castle core-runtime route: **Home → Goddess Statue → one proved information/stat detail → Statue → Home.** Follow the common lease, fresh-frame and stop protocol; record screenshots, observations and action/return trace under ignored `.local-data/`. Saved evidence comes first; no broad live tour is required.

Do not activate, bless, claim, consume items or upgrade. Missing deeper-menu evidence remains explicit. If a material route remains unproved, report the exact pending edge rather than calling the feature complete.
