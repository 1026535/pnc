# V33 — Lost City Headquarters event menus

[Index and common contract](../PNC_VISION_MODULAR_PLAN.md) · [Building coverage](BUILDING_MENU_COVERAGE.md).
Depends on V01; V02–03 for canonical slot identity and occupancy. Status: planned; support is not yet certified.

## Outcome and current evidence

The Lost City Headquarters fixed event slot, actual current endpoint and one read-only hub/detail family.

The user confirmed the building/slot exists and can be empty. The inspected catalog has no corresponding canonical object ID or current screen identity. Exact naming, location association and menu behavior remain capture gaps. Use the [versioned endpoint note](../../docs/game-reference/workflows/building-endpoints.md) as a route lead and current captured pixels as acceptance evidence.

## Canonical ownership and implementation

One new verified object ID in the existing building catalog if still missing, V03 slot proof, a feature-specific menu producer/profile/control/region set, and core navigation. Both `ObservationBuilder` and `NavigationPerception` use the same feature producer. Reuse V01's frame-bound identity/content/control contract; V02 handles Home acquisition, not menu semantics.

1. Reconcile the user's name with saved/current pixels, record one atlas slot and confirm its occupied appearance. Do not copy Dragondom's ID, coordinates or route.
2. Qualify the actual hub and current back/close control; publish visible phase/timer, progress and access facts without inventing event availability rules.
3. Implement one evidenced rules/status/reward-preview detail and its owned return. Separate any matchmaking, combat or new map subtree into a later packet with its own evidence.
4. Add or correct a scoped workflow note with source date/build, supported layouts, actual return and remaining gaps. Keep references separate from validation captures; preserve newer landed work before modifying it.

## Acceptance and bounded proof

Add current hub/detail frames and an empty/unknown slot negative through both publishers. Verify Lost City and Dragondom do not inherit one another's menus or controls. Require independent visual identity, typed observed facts, measured unoccluded controls and correct post-close state. Unknown values never inherit a prior screen's facts.

Start `py tools/run_tests.py group unit.app.pnc.vision`, then use the index's affected/integration rule for actual source changes.

One configured-testing, active-castle core-runtime route: **Testing Home → present Lost City Headquarters slot → actual hub → one proved read-only information detail → hub → Home.** Follow the common lease, fresh-frame and stop protocol; record screenshots, observations and action/return trace under ignored `.local-data/`. Saved evidence comes first; no broad live tour is required.

Do not enroll, start an event, spend currency or claim. Missing event access is a stated qualification gap; no account/castle switch or invented UI is permitted. If a material route remains unproved, report the exact pending edge rather than calling the feature complete.
